import os
import json

offline_runners = [
    {"name": "runner1", "status": "offline"},
    {"name": "runner2", "status": "offline"},
    {"name": "runner3", "status": "offline"},
    # Add more offline runners as needed
]

file_path = os.path.join("utils", "offline_runners.txt")

with open(file_path, "w") as file:
    json.dump(offline_runners, file)
