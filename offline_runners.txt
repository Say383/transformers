offline_runners = [...]  # The content of the offline_runners variable

# Step 1: Create a new file named offline_runners.txt
with open("offline_runners.txt", "w") as file:
    # Step 2: Write the content of the offline_runners variable to the file
    file.write("\n".join(offline_runners))

# Step 4: Close the file (not necessary due to the use of the `with` statement)
